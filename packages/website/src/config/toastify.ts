import { ToastContainerProps } from 'react-toastify';

const toastContainerProps: ToastContainerProps = {
  autoClose: 3000,
  closeOnClick: true,
  draggable: true,
};

export { toastContainerProps };
export default toastContainerProps;
