export const sizeWindow = `
  w-screen 
  h-screen 
`
  .replace(/\s+/g, ' ')
  .trim();

export const sizeContent = `
  py-12 
  w-full
`
  .replace(/\s+/g, ' ')
  .trim();

export const backgroundImage = `
  bg-no-repeat 
  bg-cover 
  bg-fixed bg-center
`
  .replace(/\s+/g, ' ')
  .trim();
